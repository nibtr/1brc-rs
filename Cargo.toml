[package]
name = "rs1b"
version = "0.1.0"
edition = "2024"

[profile.release]
codegen-units = 1
lto = "fat"
panic = "abort"
debug = true # for perf profiling, should remove if done

[dependencies]
rand = "0.9.2"

[target.'cfg(unix)'.dependencies]
libc = "0.2.180"
